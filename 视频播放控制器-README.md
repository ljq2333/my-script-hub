# 🎬 视频播放控制器（增强设置版）

> 一个功能丰富的 Tampermonkey / Greasemonkey 油猴脚本，为任意网页上的 HTML5 视频添加可拖拽控制面板。

---

## ✨ 功能概览

| 功能 | 说明 |
|------|------|
| 🎮 **播放/暂停** | 一键控制视频播放与暂停 |
| ⏪⏩ **快进/快退** | 支持多档位跳转（如 -10s、+10s、-1m、+1m 等） |
| 🚀 **倍速播放** | 多档倍速切换（0.5x ~ 3.0x），支持自定义倍速值 |
| 📊 **进度条** | 可拖拽的进度条，实时显示当前时间/总时长 |
| ☀️ **亮度调节** | 实时调整视频亮度（0.1x ~ 2.0x），支持重置 |
| 🔊 **音量调节** | 实时调整视频音量，支持一键静音/恢复 |
| 🔄 **水平/垂直布局** | 支持水平面板和垂直面板两种布局，一键切换 |
| 🖱️ **可拖拽面板** | 控制面板可任意拖拽到屏幕任意位置，位置自动保存 |
| 👁️ **显示/隐藏** | 一键隐藏面板，浮动按钮随时恢复显示 |
| ⚙️ **参数设置** | 可视化设置面板，调节透明度、字体大小、按钮大小 |
| 💾 **配置持久化** | 所有设置通过 `GM_setValue` 自动保存，刷新不丢失 |

---

## 📥 安装

### 前置条件
- 浏览器安装 [Tampermonkey](https://www.tampermonkey.net/) 或 [Violentmonkey](https://violentmonkey.github.io/) 扩展

### 安装步骤
1. 复制脚本内容到新油猴脚本
2. 或者直接访问安装链接（如已配置）：
   ```
   https://github.com/ljq2333/my-script-hub/raw/main/视频播放控制器.user.js
   ```
3. 保存并启用脚本
4. 打开任意包含 HTML5 视频的网页（如 B站、YouTube 等），控制面板将自动出现

---

## 🎯 使用方法

### 基本操作

| 操作 | 说明 |
|------|------|
| **▶/⏸ 按钮** | 播放 / 暂停视频 |
| **倍速按钮** (1.0x / 1.5x / ...) | 点击切换播放速度，当前倍速高亮显示 |
| **快进/快退按钮** (-10s / +10s / -1m / +1m) | 点击跳转到指定偏移 |
| **进度条** | 拖拽或点击跳转到视频任意位置 |
| **拖拽面板** | 按住面板空白区域（非按钮区域）拖拽到任意位置 |
| **✕ 按钮** | 隐藏控制面板，屏幕右下角出现浮动按钮 |
| **⚙ 按钮** | 打开参数设置面板 |

### 高级功能

| 操作 | 说明 |
|------|------|
| **☀️ 亮度** | 点击展开亮度滑块，拖动实时调节视频亮度 |
| **🔊 音量** | 点击展开音量滑块，拖动实时调节视频音量，可一键静音 |
| **↕ / ↔ 布局切换** | 在水平面板和垂直面板布局之间切换 |
| **参数设置** | 调节面板透明度、字体大小、按钮大小，支持恢复默认 |

---

## ⚙️ 自定义配置

脚本头部 `CONFIG` 对象中可直接修改默认值：

```javascript
const CONFIG = {
  TEXT: { /* 所有文字标签，可改为中文或任意语言 */ },
  DEFAULT_SPEEDS: [3.0, 2.5, 2.0, 1.5, 1.25, 1.0],  // 默认倍速档位
  DEFAULT_SEEKS: [-600, -60, -10, 10, 60, 600],       // 默认快进/快退档位（秒）
  DEFAULT_UI: {                                         // 默认界面样式
    opacity: 0.85,    // 面板透明度
    fontSize: 14,     // 字体大小
    btnSize: 14,      // 按钮大小（垂直布局）
    brightness: 1.0,  // 亮度
    volume: 1.0,      // 音量
  },
};
```

也可以通过可视化 **⚙ 参数设置** 面板实时调整，无需手动修改代码。

---

## 📂 文件说明

| 文件 | 说明 |
|------|------|
| `视频播放控制器.user.js` | **最新稳定版** (v0.8.2) — 推荐使用 |
| `视频播放控制器（增强设置版）-0.5.user.js` | 旧版本，含早期增强功能 |
| `docs/superpowers/plans/2026-05-29-video-controller-optimization.md` | 后续优化计划文档 |

---

## 🔧 技术细节

- **纯前端**：零依赖，纯原生 JavaScript 实现
- **API 使用**：`GM_setValue` / `GM_getValue` 持久化配置
- **视频探测**：智能评分算法，自动选择页面中最佳视频元素（支持 iframe 和 Shadow DOM）
- **响应式**：支持动态插入的视频（MutationObserver 监听），视频切换时自动重建面板
- **兼容性**：支持所有主流浏览器上的 HTML5 视频播放器

---

## 🗺️ 开发计划

参见 [优化计划文档](docs/superpowers/plans/2026-05-29-video-controller-optimization.md)，计划包括：

- [ ] 添加键盘快捷键支持（方向键快进/快退、± 键调节倍速）
- [ ] 代码模块化重构，消除水平/垂直面板的重复代码
- [ ] 优化视频查找性能
- [ ] 改进拖拽视觉反馈

---

## 📝 更新日志

### v0.8.2
- 新增亮度调节功能（☀️）
- 新增音量调节功能（🔊），支持一键静音
- 新增垂直面板布局（↕）
- 设置面板增加按钮大小调节
- 优化视频查找逻辑和 UI 交互

### v0.5
- 初始增强版本，支持参数设置面板
- 可自定义倍速/快进参数
- 面板透明度、字体大小可调

---

## 📄 许可证

本项目仅供个人学习使用。
